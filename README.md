## Codebase démonstration

```bash
docker compose build --no-cache
docker compose up -d
```

Ouvrir [http://localhost:3000](http://localhost:3000) dans votre navigateur.

### Pour les pages protégées:

- Email: test@test.com
- Password: password
